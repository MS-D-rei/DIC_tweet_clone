<h1>Tweet List</h1>
<table>
  <% @posts.each do |post| %>
  <tr>
    <td><%= post.content %></td>
    <td><%= link_to 'edit', edit_post_path(post.id) %></td>
  </tr>
  <% end %>
</table>
<%= link_to 'create tweet', new_post_path, class: 'btn btn-primary' %>