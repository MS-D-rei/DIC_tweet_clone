<%= form_with(model: @post, local: true, url: action_based) do |f| %>

<%= f.label :content %>
<%= f.text_field :content, class: 'form-control' %>

<%= f.submit 'submit', class: 'btn btn-primary' %>
<% end %>